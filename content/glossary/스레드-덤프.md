---
term: 스레드 덤프
aliases: [thread dump, jstack]
link: deadlock
---

그 순간 JVM의 **모든 스레드가 어디서 무엇을 하고 있는지** 찍은 스냅숏이다. `jstack`으로 뜬다. `BLOCKED`는 모니터 락을 기다리는 중, `WAITING`은 다른 스레드의 신호(`park`, `wait`)를 기다리는 중이다. 교착이 있으면 자동으로 알려 준다.
